package fpml.consolidated.cd.validation;

import com.google.common.collect.Lists;
import com.rosetta.model.lib.expression.ComparisonResult;
import com.rosetta.model.lib.path.RosettaPath;
import com.rosetta.model.lib.validation.ValidationResult;
import com.rosetta.model.lib.validation.Validator;
import fpml.consolidated.cd.IndexAnnexSource;
import java.util.List;

import static com.google.common.base.Strings.isNullOrEmpty;
import static com.rosetta.model.lib.expression.ExpressionOperatorsNullSafe.checkCardinality;
import static com.rosetta.model.lib.validation.ValidationResult.failure;
import static com.rosetta.model.lib.validation.ValidationResult.success;
import static java.util.stream.Collectors.toList;

public class IndexAnnexSourceValidator implements Validator<IndexAnnexSource> {

	private List<ComparisonResult> getComparisonResults(IndexAnnexSource o) {
		return Lists.<ComparisonResult>newArrayList(
				checkCardinality("value", (String) o.getValue() != null ? 1 : 0, 1, 1), 
				checkCardinality("indexAnnexSourceScheme", (String) o.getIndexAnnexSourceScheme() != null ? 1 : 0, 0, 1)
			);
	}

	@Override
	public List<ValidationResult<?>> getValidationResults(RosettaPath path, IndexAnnexSource o) {
		return getComparisonResults(o)
			.stream()
			.map(res -> {
				if (!isNullOrEmpty(res.getError())) {
					return failure("IndexAnnexSource", ValidationResult.ValidationType.CARDINALITY, "IndexAnnexSource", path, "", res.getError());
				}
				return success("IndexAnnexSource", ValidationResult.ValidationType.CARDINALITY, "IndexAnnexSource", path, "");
			})
			.collect(toList());
	}

}
