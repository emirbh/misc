package fpml.consolidated.shared.validation;

import com.google.common.collect.Lists;
import com.rosetta.model.lib.expression.ComparisonResult;
import com.rosetta.model.lib.path.RosettaPath;
import com.rosetta.model.lib.validation.ValidationResult;
import com.rosetta.model.lib.validation.Validator;
import fpml.consolidated.shared.ContactInformation;
import java.util.List;

import static com.google.common.base.Strings.isNullOrEmpty;
import static com.rosetta.model.lib.validation.ValidationResult.failure;
import static com.rosetta.model.lib.validation.ValidationResult.success;
import static java.util.stream.Collectors.toList;

public class ContactInformationTypeFormatValidator implements Validator<ContactInformation> {

	private List<ComparisonResult> getComparisonResults(ContactInformation o) {
		return Lists.<ComparisonResult>newArrayList(
			);
	}

	@Override
	public List<ValidationResult<?>> getValidationResults(RosettaPath path, ContactInformation o) {
		return getComparisonResults(o)
			.stream()
			.map(res -> {
				if (!isNullOrEmpty(res.getError())) {
					return failure("ContactInformation", ValidationResult.ValidationType.TYPE_FORMAT, "ContactInformation", path, "", res.getError());
				}
				return success("ContactInformation", ValidationResult.ValidationType.TYPE_FORMAT, "ContactInformation", path, "");
			})
			.collect(toList());
	}

}
