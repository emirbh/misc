package fpml.consolidated.business.events.validation;

import com.google.common.collect.Lists;
import com.rosetta.model.lib.expression.ComparisonResult;
import com.rosetta.model.lib.path.RosettaPath;
import com.rosetta.model.lib.validation.ValidationResult;
import com.rosetta.model.lib.validation.Validator;
import fpml.consolidated.business.events.RequestedClearingAction;
import java.util.List;

import static com.google.common.base.Strings.isNullOrEmpty;
import static com.rosetta.model.lib.expression.ExpressionOperatorsNullSafe.checkCardinality;
import static com.rosetta.model.lib.validation.ValidationResult.failure;
import static com.rosetta.model.lib.validation.ValidationResult.success;
import static java.util.stream.Collectors.toList;

public class RequestedClearingActionValidator implements Validator<RequestedClearingAction> {

	private List<ComparisonResult> getComparisonResults(RequestedClearingAction o) {
		return Lists.<ComparisonResult>newArrayList(
				checkCardinality("value", (String) o.getValue() != null ? 1 : 0, 1, 1), 
				checkCardinality("requestedClearingActionScheme", (String) o.getRequestedClearingActionScheme() != null ? 1 : 0, 0, 1)
			);
	}

	@Override
	public List<ValidationResult<?>> getValidationResults(RosettaPath path, RequestedClearingAction o) {
		return getComparisonResults(o)
			.stream()
			.map(res -> {
				if (!isNullOrEmpty(res.getError())) {
					return failure("RequestedClearingAction", ValidationResult.ValidationType.CARDINALITY, "RequestedClearingAction", path, "", res.getError());
				}
				return success("RequestedClearingAction", ValidationResult.ValidationType.CARDINALITY, "RequestedClearingAction", path, "");
			})
			.collect(toList());
	}

}
